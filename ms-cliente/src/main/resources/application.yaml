server:
  port: 8081

spring:
  application:
    name: ms-cliente
  datasource:
    url: jdbc:h2:mem:clientedb
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true